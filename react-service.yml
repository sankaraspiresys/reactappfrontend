apiVersion: v1
kind: Service
metadata:
  name: react-service
  labels:
    app: react-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30005
  selector:
    app: reactapp
    type: frontend